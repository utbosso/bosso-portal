name: Scrape TeamWork Internships

on:
  schedule:
    - cron: '30 12 * * *' # 12:30 UTC ~ early morning US time
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TEAMWORK_SEARCH_URL: ${{ secrets.TEAMWORK_SEARCH_URL }}
        run: npm run scrape:teamwork -- --push --limit=50
